# Core PySpark and Data Handling
pyspark==4.0.1
pandas==2.1.4
pyarrow>=4.0.0

# Airflow Orchestration (Core and Providers)
apache-airflow==2.9.2
apache-airflow-providers-apache-spark==5.3.0
apache-airflow-providers-mongo==4.2.0

# NLP and Text Processing
spacy==3.7.4
# Note: You must run 'python -m spacy download en_core_web_sm' separately
# The library for the simplified solution is spacy, not medspacy.

# MongoDB Connection
pymongo==4.6.2